#define DEF_VAULT_VERSION_TAG "@VERSIONTAG@"
#define DEF_VAULT_VERSION "0.18.1.0"
#define DEF_VAULT_RELEASE_NAME "Fluorine Fermi"
#define DEF_VAULT_VERSION_FULL DEF_VAULT_VERSION "-" DEF_VAULT_VERSION_TAG
#define DEF_VAULT_VERSION_IS_RELEASE @VERSION_IS_RELEASE@

#include "version.h"

const char* const VAULT_VERSION_TAG = DEF_VAULT_VERSION_TAG;
const char* const VAULT_VERSION = DEF_VAULT_VERSION;
const char* const VAULT_RELEASE_NAME = DEF_VAULT_RELEASE_NAME;
const char* const VAULT_VERSION_FULL = DEF_VAULT_VERSION_FULL;
const bool VAULT_VERSION_IS_RELEASE = DEF_VAULT_VERSION_IS_RELEASE;
